# URL base da API (deve terminar em /api). Embutida no build — altere e rode `npm run build` de novo.
# Dev local:
VITE_API_URL=http://localhost:3001/api
# Produção (ex.: app Node na Hostinger — use a URL pública exata do painel):
# VITE_API_URL=https://seu-subdominio.hostingersite.com/api
